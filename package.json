{
  "name": "liquidbranding",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev public --port 8788",
    "deploy": "npx wrangler pages deploy public --project-name liquidbranding",
    "deploy:worker": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
